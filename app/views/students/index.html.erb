<%= form_tag "/search" do %>
<%= label_tag "search", "Search" %>
<%= search_field_tag(:q) %>
<%= submit_tag "Search" %>
<% end %>

<%= @students.each do |student| %>
<%= render partial: "student", locals: { student: student } %>
<% end %>
